{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "A web-based weather application that allows users to enter a location and retrieve the current weather conditions for that location.",
  "main": "uiserver.js",
  "scripts": {
    "start": "nodemon -w . uiserver.js",
    "compile": "babel src --out-dir public --watch --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Prithik Vijayakumar",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^3.13.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "graphql": "^16.9.0",
    "mongodb": "^6.11.0",
    "nodemon": "^3.1.7"
  },
  "devDependencies": {
    "@babel/cli": "^7.25.9",
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "@babel/preset-react": "^7.25.9"
  }
}
